import './Login.css';

/**
 * Cmp Login
 * @module Login
 */
const Login = () => {
    
    const handleLogin = async (e) => {
        // TODO: impl
    };
    
    return (
        <div>
            <h1>Login</h1>
            <form onSubmit={handleLogin}>
                {/* TODO: impl */}
            </form>
        </div>
    );
};

export default Login;